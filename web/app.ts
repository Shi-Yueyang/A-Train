// A-Train browser client. Implemented in Phase 5.
//
// The UI communicates only with the simulator (REST + WebSocket) and contains
// no simulation rules. It displays train state, signals, BTM, ATP state,
// digital I/O, and faults using the latest WebSocket snapshot.
